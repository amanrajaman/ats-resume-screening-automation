# ats-resume-screening-automation
Automated resume screening system built in Python that extracts PDF/DOCX resumes, scores candidates using keyword matching and NLP semantic similarity (spaCy), ranks them, generates Excel reports, and emails results to HR.
